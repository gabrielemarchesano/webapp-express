# Setup Webapp Express
Creare un'app di `film` con relative `recensioni`

## Steps
1. create schema and er-diagram [√]
2. import and create the database on Workbench [√]
3. generate a list of movies and reviews to add to the db [√]
4. create the express app [√]
  1. add body parser [√]
  2. add middleware assets static [√]
  3. link the app to the database and test it [√]
  4. CRUD: add router + controller [√]
  5. add midlewares notFound and serverError. []
